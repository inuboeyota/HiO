export async function messageCommandPermission(message, msgLoadedCommands) {
    // prefix部分を抜粋する
    const prefix = message.content.replace("　", " ").split(' ')[0];
    // 発言者の情報を取得する
    return true;
}
